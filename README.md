This project is a simple log reader written in C#
To work properly you should initialize a database in your local server(by adjusting the connection string) and then run the programm and find the information that you need from logs
